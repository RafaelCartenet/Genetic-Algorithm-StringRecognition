Première fois :
1) créé un dossier ou copier le projet distant
2) placer dans se dossier puis :
3) relier le dossier existant au dossier github en ligne :

git init
git remote add origin https://github.com/Rafafouille76/GAstringcpp


RECUPERER :
1) se mettre dans le dossier créé
git pull -u origin master


PUSH :
Nouveaux fichiers :
git add FICHIER ( se placer à l'emplacement de FICHIER)
git commit -am "explication du push"
git push -u origin master

Fichiers existants :
git commit -am "explication du push"
git push -u origin master



